class Level:

    def __init__(self, number, start_range, end_range, max_attempts):
        self.number = number
        self.start_range = start_range
        self.end_range = end_range
        self.max_attempts = max_attempts
